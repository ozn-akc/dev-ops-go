name: Docker Build and Push

on:
  push:
    tags:
      - '*'

jobs:
  docker:
    runs-on: ubuntu-latest
    steps:
      - name: Get service name and version
        id: service
        run: echo Hallo